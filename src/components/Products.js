import React, { Component } from 'react';

class Products extends Component {
  render() {
    return (
    	<div className="wrap">
    		<h1>Products</h1>
    		{/*
    		<p>{this.props.loadData.data1.desc}</p>
    		<div>
    			{
						Object
							.keys(this.props.loadData)
					}
    		</div>
    		*/}

    	</div>
    );
  }
}

export default Products;


// 	Object.keys($r.props.loadData)